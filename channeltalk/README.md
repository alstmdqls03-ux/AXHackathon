# 채널톡 Codex 플러그인 — (플러그인 이름 미정)

> 이 파일은 submission.zip 루트의 README가 된다. 문항 3(작동 방식)의 세부 내용과 일치해야 한다.

## 개요

(한 문장: 무엇을 하는 플러그인인지 — 누가, 어떤 상황에서 쓰는지)

## 설치·실행 (심사자용)

1. `src/`를 `plugins/<플러그인이름>/`으로 복사 (또는 `~/.codex/plugins/`)
2. `.agents/plugins/marketplace.json`(repo 기준) 또는 `~/.agents/plugins/marketplace.json`(personal)에 경로 등록
3. Codex 재시작 후 실행: (예시 프롬프트/명령 기재)

## 작동 방식

(절차 / 사용하는 지식 / 판단 기준 / 정보가 부족하거나 잘 안 풀리는 상황에서의 동작)

## 검증

(입력 → 결과 예시 1개 이상, 정상 케이스와 예외 케이스)
