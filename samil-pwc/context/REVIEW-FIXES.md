# REVIEW-FIXES.md — AI 심사 시뮬레이션 v1 결과 반영 (2026-07-10)

> AI 심사위원 채점: **93/100**. 아래 반영으로 1등 기준을 굳힌다.
> 불변 규칙: logs/ 및 *.jsonl 내용 수정·삭제 절대 금지 (P1-3의 "무편집 이동"만 허용된 처리). question.md 자수 제한 유지, 수정 후 재측정.

## P0-1 — 문항 4 ↔ 문항 2 내부 모순 (심사자가 바로 잡는 급소)

문항 4 (a)는 "403 차단 → KDI 공공 미러로 교체"인데, **문항 2의 실제 URL 목록에는 KDI가 없고 한국세정신문 보도(taxtimes)가 있다** (KDI 미러 타임아웃으로 재교체한 경위는 zip 미포함 파일에만 있음).
→ 문항 4 (a)를 최종 상태로 수정: "1차로 KDI 공공 미러로 교체했으나 재확인 시 미러가 불안정해, 동일 통계의 보도 원문(한국세정신문)으로 최종 교체" 수준 (자수 여유 확인).

## P0-2 — 통화 단위 선언 (F-01 서술 vs 데이터 불일치)

README·문항 5는 "수선비 8.4억"인데 데이터·체커 출력의 원시값은 `840`이고 **src/ 어디에도 단위(백만원) 선언이 없다.**
→ SKILL.md 입력 계약과 README 데모 설명에 **"데모 금액 단위: 백만원"** 1줄 명시 (CSV·체커는 수정 불필요).

## P0-3 — 로그 1차 증거 보강 (전사 공통 급소)

훅은 대화 텍스트만 저장한다 — 정상 19건·예외 H2 skip의 원시 출력이 제출 logs/에 없다.
→ 이 세션에서 체커 검증(정상 + 예외)을 **다시 실제 실행**하고, **명령 원문과 핵심 출력(후보 19건, H1-CAP-4712 등 심은 신호 ID, H2 skip 사유, exit 0)을 너의 응답 텍스트에 그대로 인용**하라 — 훅 로그에 1차 증거로 남도록.

## P1

1. README 검증 절 정확화: "시산표 제조간접비 4,200" → "시산표 간접원가 4계정 합(간접노무비+감가상각비+수선비+기타제조경비) 4,200" (시산표에 '제조간접비' 계정은 없음).
2. `context/logs/claude-code/`의 훅 스냅샷 1건을 **동일한 무편집 이동 절차**(cp → cmp byte-identical → 원위치 제거)로 `logs/stray/claude-code-context-snapshot/` 등으로 이관하고 `logs/stray/README.txt`에 항목 추가.
3. checks.py의 미사용 SYNONYMS 딕셔너리(dead code) 삭제 또는 주석 정리 — 문항 3 "동의어 매핑" 서술은 SKILL.md(에이전트 몫)로 명확화.

## P2

4. 사용자 정의 순서 통일: README(CFO·재무팀 우선) ↔ 문항 1(삼일 회계사·컨설턴트 우선) 한쪽으로 정렬.
5. README에 "실행 시 출력 예" 코드블록으로 체커 stdout 원문 일부 수록 (심사자가 기대 출력을 즉시 대조).
6. 문항 1 "수 주간 전수 교차 검토" 서술에 문항 2 통계(정정 공시·감리 사후 적발)와의 연결 한 구절 명시.

## 완료 보고

전 항목 반영 후 자수 재측정 결과와 함께 `context/JOURNAL.md`에 `[step-07] 심사 반영 수정` 규격 보고 후 정지.
